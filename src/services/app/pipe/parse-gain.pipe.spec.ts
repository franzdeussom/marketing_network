import { ParseGainPipe } from './parse-gain.pipe';

describe('ParseGainPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseGainPipe();
    expect(pipe).toBeTruthy();
  });
});
